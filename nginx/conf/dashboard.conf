server {
    # Helps if a proxy is infront of us
    absolute_redirect off;

    # Listen parameters
    include /var/run/fx/nginx/dashboard/iface.conf;

    # Web server
    include /etc/nginx/fx/dashboard_web.conf;
}
